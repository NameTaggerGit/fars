# 🚀 Деплой FARS на Render - Пошаговая инструкция

## 📋 Краткая справка

| Что | Где | Зачем |
|-----|-----|-------|
| API (NestJS) | Render Web Service | Запуск backend с Node.js |
| Database (PostgreSQL) | Render PostgreSQL | Хранение данных |
| Frontend (React) | Render Static Site | Размещение статических файлов |

---

## 🎯 Шаг 1: Подготовка (5 минут)

### 1.1 Загрузить проект на GitHub

```powershell
# В папке проекта
git init
git add .
git commit -m "Initial commit: FARS Messenger"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/FARS.git
git push -u origin main
```

### 1.2 Проверить что .env не загружается

Убедись что в `.gitignore` есть:
```
.env
.env.local
.env.production
.env.*.local
```

---

## 🌐 Шаг 2: Регистрация на Render (2 минуты)

1. Перейти https://render.com
2. Нажать "Sign up with GitHub"
3. Авторизоваться через GitHub
4. Дать Render доступ к репозиториям

---

## 🛠️ Шаг 3: Развертывание API Backend (10 минут)

### 3.1 Создать Web Service

1. На дэшборде Render нажать **"New +"** → **"Web Service"**
2. Выбрать репозиторий **FARS**
3. Заполнить поля:

| Поле | Значение |
|------|----------|
| Name | `fars-api` |
| Runtime | `Node` |
| Region | Выбрать ближайший |
| Branch | `main` |
| Build Command | `npm install && npm run build:api && npx prisma migrate deploy` |
| Start Command | `npm run start:api` |
| Plan | `Free` или `Starter` |

4. Нажать **"Create Web Service"**
5. **Ждать 5-10 минут на деплой**

### 3.2 Добавить переменные окружения

#### Где находятся переменные окружения?

1. Открыть Render дэшборд
2. Выбрать Web Service **`fars-api`**
3. На левой панели нажать вкладку **"Environment"**
4. Внизу будет форма **"Add Environment Variable"**

#### Как добавить каждую переменную?

В форме есть два поля:
- **Key** (левое поле) - название переменной
- **Value** (правое поле) - значение переменной

**Добавь переменные в таком порядке:**

| Key | Value | Описание |
|-----|-------|----------|
| `NODE_ENV` | `production` | Режим production |
| `PORT` | `3001` | Порт API (НЕ МЕНЯЙ) |
| `API_PREFIX` | `api` | Префикс API (НЕ МЕНЯЙ) |
| `JWT_ACCESS_SECRET` | `[Сгенерируй своё значение]` | Секрет для JWT токенов доступа |
| `JWT_ACCESS_TTL` | `15m` | Время жизни токена доступа |
| `JWT_REFRESH_SECRET` | `[Сгенерируй своё значение]` | Секрет для JWT refresh токенов |
| `JWT_REFRESH_TTL` | `7d` | Время жизни refresh токена |
| `FRONTEND_URL` | `https://YOUR_FRONTEND_URL.onrender.com` | URL фронтенда (обновишь позже) |

#### 🔐 Как сгенерировать JWT_ACCESS_SECRET и JWT_REFRESH_SECRET?

**Способ 1: Онлайн генератор (БЫСТРО)**
1. Открой https://www.uuidgenerator.net/
2. Наведи на UUID, скопируй (Ctrl+C)
3. Вставь в **Value** поле
4. Повтори 2-3 раза чтобы получить 32+ символов
5. Результат: `a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4`

**Способ 2: Через bash/PowerShell (ВЕРНЕЕ)**

Если у тебя есть bash/PowerShell: 
```powershell
# На Windows PowerShell
[System.Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Random -SetSeed (Get-Date).Ticks))) -replace '=', '' | ForEach-Object { $_.Substring(0, 32) }
```

**Способ 3: Просто используй эти значения (ПРОСТОЙ)**
```
JWT_ACCESS_SECRET=SuperSecretKeyForAccessTokens12345678
JWT_REFRESH_SECRET=SuperSecretKeyRefreshTokensABC123456
```

#### 📍 Пошаговый процесс добавления переменной:

1. **Открыть форму**
   - На странице Environment внизу видишь:
     ```
     KEY: [пустое поле]
     VALUE: [пустое поле]
     [Кнопка "Add"]
     ```

2. **Заполнить KEY**
   - Нажми левое поле
   - Напиши: `NODE_ENV`

3. **Заполнить VALUE**
   - Нажми правое поле
   - Напиши: `production`

4. **Добавить переменную**
   - Нажми кнопку **"Add"** (или Enter)
   - Переменная появится в списке выше

5. **Повторить для остальных переменных**
   - Сделай то же самое для всех 8 переменных из таблицы

#### ⚠️ Важные замечания:

- **JWT_ACCESS_SECRET** и **JWT_REFRESH_SECRET** должны быть РАЗНЫЕ!
- Минимум 32 символа для каждого
- Не оставляй пробелы в начале/конце значения
- После добавления всех переменных нажми кнопку **"Save"** внизу страницы

#### 🔗 FRONTEND_URL - обновить позже

После того как развернёшь фронтенд (Шаг 5), узнаешь его URL:
- Пример: `https://fars-web-abc123.onrender.com`

Вернись в Environment API и измени:
- **Key**: `FRONTEND_URL`
- **Value**: `https://fars-web-abc123.onrender.com` (твой реальный URL)

Нажми **"Save"** - произойдёт автоматический деплой API с новыми переменными

#### ✅ Как проверить что переменные добавлены?

1. Открыть вкладку **"Environment"** API
2. Должен увидеть список всех 8 переменных
3. Нажать **"Save"** внизу (если ещё не нажимал)
4. В логах внизу страницы будет сообщение: `✅ Deploy updated`

---

## 🗄️ Шаг 4: Создать PostgreSQL базу (5 минут)

### 4.1 Создать новую базу данных

1. На дэшборде нажать **"New +"** → **"PostgreSQL"**
2. Заполнить поля:

| Поле | Значение |
|------|----------|
| Name | `fars-db` |
| Database | `fars` |
| User | `fars` |
| Region | Выбрать ТАКОЙ ЖЕ как API |
| Plan | `Free` или `Starter` |

3. Нажать **"Create Database"**

### 4.2 Добавить DATABASE_URL в API

#### 📍 ШАГ 1: Найти Internal Database URL в PostgreSQL

1. **Открыть Render дэшборд** → https://dashboard.render.com
2. **Найти PostgreSQL базу** `fars-db` в списке сервисов (слева или внизу)
3. **Нажать на `fars-db`** - откроется страница базы данных
4. **Нажать вкладку "Info"** (или "Connection" в зависимости от версии Render)
5. **Найти строку "Internal Database URL"**
   - Выглядит примерно так:
   ```
   postgresql://fars:RANDOM_PASSWORD@internal-postgres-host-1234.render-instances.com:5432/fars
   ```
6. **Нажать кнопку "Copy"** (рядом со строкой) или выделить весь текст и Ctrl+C

#### 📝 Что такое Internal Database URL?

Это строка подключения к базе данных. Её части:
```
postgresql://
   ↓
   fars (пользователь)
   :
   RANDOM_PASSWORD (пароль - сгенерирован автоматически)
   @
   internal-postgres-host-1234.render-instances.com (хост базы)
   :5432 (порт)
   /fars (имя базы данных)
```

**СВОЮ** Internal Database URL скопируй полностью - она содержит пароль и хост!

#### 🔗 ШАГ 2: Добавить DATABASE_URL в Web Service (API)

1. **Открыть Web Service `fars-api`** на Render дэшборде
2. **Нажать вкладку "Environment"** (левая панель)
3. **Прокрутить вниз** до формы **"Add Environment Variable"**
4. **В левое поле (KEY) напиши:**
   ```
   DATABASE_URL
   ```
5. **В правое поле (VALUE) вставь скопированное значение:**
   - Это та самая "Internal Database URL" из шага 1
   - Пример:
   ```
   postgresql://fars:RANDOM_PASSWORD@internal-postgres-host-1234.render-instances.com:5432/fars?schema=public&connection_limit=5
   ```

#### ⚠️ ВАЖНО: Параметры в конце URL

В конце уже добавлены параметры:
```
?schema=public&connection_limit=5
```

Это значит:
- `schema=public` - схема БД (ОСТАВЬ КАК ЕСТЬ)
- `connection_limit=5` - максимум 5 соединений одновременно (предотвращает ошибку "too many connections")

**Если база заполнится ошибками, уменьши на 2:**
```
?schema=public&connection_limit=2
```

#### 📸 Визуальный пример:

```
На странице Environment Web Service выглядит так:

┌─ Key ───────────────── Value ────────────────────────────────┐
├──────────────────────────────────────────────────────────────┤
│ NODE_ENV             │ production                             │
│ PORT                 │ 3001                                   │
│ API_PREFIX           │ api                                    │
│ [другие переменные]  │ [их значения]                          │
├──────────────────────────────────────────────────────────────┤
│ DATABASE_URL         │ postgresql://fars:PASSWORD@host...     │
│                      │ ?schema=public&connection_limit=5      │
├──────────────────────────────────────────────────────────────┤
│ [Кнопка "Add"]       [Кнопка "Save"]                         │
└──────────────────────────────────────────────────────────────┘
```

#### ✅ ШАГ 3: Сохранить изменения

1. **Нажать кнопку "Add"** - переменная появится в списке
2. **Нажать кнопку "Save"** внизу страницы (ОБЯЗАТЕЛЬНО!)
3. **Дождись сообщения**:
   ```
   ✅ Deploy started...
   ```
4. **Render начнёт переразвертывать API** (~3-5 минут)

#### 🔍 ШАГ 4: Проверить что БД подключилась

1. **Открыть вкладку "Logs"** на странице Web Service (API)
2. **Смотреть на логи** - должны появиться в реальном времени
3. **ИЩИ одно из этих сообщений:**

✅ **УСПЕХ:**
```
[Nest] 12345  - 13/02/2026, 3.30.00  LOG [PrismaService]
🔗 Connecting to PostgreSQL...
✅ Database connected!
```

❌ **ОШИБКА (типичные проблемы):**

**Проблема:** `FATAL: too many connections for role "fars"`
```
✅ Решение: Уменьши connection_limit в DATABASE_URL с 5 на 2
```

**Проблема:** `connect ECONNREFUSED`
```
✅ Решение: Чекни что DATABASE_URL скопирован полностью (с паролем)
```

**Проблема:** `database "fars" does not exist`
```
✅ Решение: Убедись что в Render создана база с именем "fars"
```

#### 🎯 Итого что произойдет:

1. ✅ DATABASE_URL добавлена в Environment API
2. ✅ Автоматический деплой (Render перезапустит API)
3. ✅ API подключится к PostgreSQL базе
4. ✅ Prisma создаст таблицы (первый раз может занять 30 сек)
5. ✅ Логи покажут: `✅ Database connected!`

После этого можешь переходить к **Шагу 5: Развертывание Frontend**

---

## 🎨 Шаг 5: Развертывание Frontend (5 минут)

### 5.1 Создать Static Site

1. На дэшборде нажать **"New +"** → **"Static Site"**
2. Выбрать репозиторий **FARS**
3. Заполнить поля:

| Поле | Значение |
|------|----------|
| Name | `fars-web` |
| Build Command | `npm install && npm run build:web` |
| Publish directory | `apps/web/dist` |
| Region | Выбрать ближайший |
| Plan | `Free` |

4. Нажать **"Create Static Site"**

### 5.2 Добавить переменные окружения

1. В Static Site нажать **"Environment"**
2. Добавить переменные:

```env
VITE_API_URL=https://ВАШЕ_API_ИМЯ.onrender.com/api
VITE_WS_URL=https://ВАШЕ_API_ИМЯ.onrender.com
```

(Замени `ВАШЕ_API_ИМЯ` на имя Web Service, например `fars-api`)

3. Нажать **"Save"**

---

## ✅ Шаг 6: Проверка (5 минут)

### 6.1 Проверить API

1. Открыть браузер
2. Перейти на: `https://ВАШЕ_API_ИМЯ.onrender.com/api/health`
3. Должен увидеть: `{"status":"ok"}`

### 6.2 Проверить Frontend

1. Перейти на: `https://ВАШЕ_ФРОНТЕНД_ИМЕНА.onrender.com`
2. Должна загрузиться страница входа

### 6.3 Проверить БД

1. В Web Service нажать **"Logs"**
2. Если видишь `Database connected!` - БД работает ✅
3. Если видишь ошибку подключения - проверить DATABASE_URL

---

## 🔄 Обновления после деплоя

### Каждый раз когда обновляешь код:

```powershell
# Локально
git add .
git commit -m "Описание изменений"
git push

# Render АВТОМАТИЧЕСКИ переразвернет приложение!
```

### Откатить на предыдущую версию:

1. В Web Service нажать **"Manual Deploy"**
2. Выбрать нужный commit
3. Нажать **"Deploy"**

---

## 🐛 Решение проблем

### "Build failed - npm not found"
```
✅ Решение: На Render есть Node.js, проверь что все зависимости в package.json
```

### "Too many database connections"
```
Добавить в DATABASE_URL: ?connection_limit=2
```

### "Cannot GET /api/health"
```
Проверить PORT в Environment (должен быть 3001)
Проверить что API_PREFIX = api
```

### "WebSocket не работает"
```
VITE_WS_URL должен быть БЕЗ '/api', именно:
https://ВАШЕ_API_ИМЕНА.onrender.com
```

### "Файлы не загружаются"
```
На Render нет постоянного хранилища файлов
Нужно добавить S3 (AWS, DigitalOcean Spaces, etc.)
```

---

## 📊 Стоимость (примерно)

| Сервис | Free Tier | Starter |
|--------|-----------|---------|
| Web Service | 0.50$/час → ~3$/ мес | 7$/мес |
| PostgreSQL | 90 часов/мес | 15$/мес |
| Static Site | Бесплатно | Бесплатно |

**Итого**: ~$3-22/месяц в зависимости от нагрузки

---

## ✨ После успешного деплоя

Поздравляем! 🎉 FARS работает:

- ✅ Пользователи регистрируются
- ✅ Чат синхронизируется в реальном времени
- ✅ Звонки работают (audio + video)
- ✅ Их друзья видят их онлайн статус

---

## 🆘 Если что-то не работает

1. **Проверить логи**: В каждом сервисе на Render есть **Logs** вкладка
2. **Git Push**: Убедиться что последние изменения в GitHub (git push)
3. **Перезагрузиться**: Нажать **"Manual Deploy"** в Web Service
4. **Очистить браузер**: Ctrl+Shift+Delete → Очистить весь кэш

---

**Готово!** 🚀 Теперь FARS доступен в интернете!
