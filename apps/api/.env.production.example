# ============ API (apps/api) - PRODUCTION ============
NODE_ENV=production
PORT=3001
API_PREFIX=api

# Database - Render provides this automatically via DATABASE_URL
# But you can override it here
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public&connection_limit=5"

# Redis (optional, for caching)
REDIS_URL=redis://localhost:6379

# JWT - Generate strong random strings (min 32 chars)
JWT_ACCESS_SECRET=your-super-secret-access-key-min-32-chars-generate-random
JWT_ACCESS_TTL=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-generate-random
JWT_REFRESH_TTL=7d

# Frontend URL (for CORS)
FRONTEND_URL=https://fars-web.onrender.com

# Email (Nodemailer) - Optional
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@fars.app

# Storage (S3-compatible) - Optional
S3_ENDPOINT=https://s3.amazonaws.com
S3_BUCKET=your-bucket-name
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_REGION=us-east-1

# ============ Web (apps/web) ============
VITE_API_URL=https://fars-api.onrender.com/api
VITE_WS_URL=https://fars-api.onrender.com
